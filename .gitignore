# Python
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Env / secrets
.env

# Models & data (large / private)
# Anchored to repo root so the ONNX model dir is ignored but the app/models
# Python package (ORM entities) is NOT.
/models/
*.onnx
/data/

# OS
.DS_Store

TASKS.md
faces/