# ---- Web / API ----
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
python-multipart==0.0.20

# ---- Auth / Security ----
python-jose[cryptography]==3.3.0
passlib==1.7.4
# Pinned: passlib 1.7.4 is incompatible with bcrypt>=4.1 (backend version detection).
bcrypt==4.0.1
slowapi==0.1.9

# ---- Database ----
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0
pgvector==0.3.6
greenlet==3.1.1

# ---- Distributed state (job store + rate limiters); optional unless REDIS_URL set ----
redis==5.2.1

# ---- ML / CV ----
# 1.26.0 ships CUDA 12.x kernels with Blackwell/sm_120 support (RTX 50-series).
# 1.20.1 silently fell back to CPU on sm_120.
onnxruntime-gpu==1.26.0
# CUDA 12 + cuDNN 9 runtime libs (loaded via ort.preload_dlls()). Drop these
# only if the host/Docker image already provides CUDA 12.x + cuDNN 9 on the path.
nvidia-cublas-cu12==12.9.2.10
nvidia-cudnn-cu12==9.23.1.3
nvidia-cuda-runtime-cu12==12.9.79
nvidia-cufft-cu12==11.4.1.4
nvidia-curand-cu12==10.3.10.19
numpy==1.26.4
opencv-python-headless==4.10.0.84
scikit-image==0.25.0
Pillow==11.0.0

# ---- Utils ----
structlog==24.4.0
orjson==3.10.13
tenacity==9.0.0

# ---- Tests ----
pytest==8.3.4
pytest-asyncio==0.25.0
httpx==0.28.1
